const modeling = require('@jscad/modeling');

exports.cubeRaw = opts => modeling.primitives.cube(opts);
exports.cylinderRaw = opts => modeling.primitives.cylinder(opts);
exports.ellipseRaw = opts => modeling.primitives.ellipse(opts);
